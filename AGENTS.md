# 项目协作规范

## 1. 每次启动必读

本文件为 Kimi Code 的 AGENTS.md，每次新会话会自动读取。所有与本项目相关的代码修改、行为规范必须遵循此文件。

## 2. 代码修改规范

### 2.1 修改前
- 明确本次修改目标，能用一句话描述清楚。
- 若改动涉及多个模块，先简要列出影响范围。
- 涉及用户未明确说明的实现方式时，先简要说明方案再动手。

### 2.2 修改中
- 最小化改动：只改与目标相关的代码，不顺便重构无关内容。
- 保持与周围代码风格一致（命名、缩进、注释密度、语言习惯）。
- 不引入项目未声明的依赖；若必须引入，先向用户说明。
- 不修改测试逻辑本身；只修复因接口变更导致的测试错误。

### 2.3 修改后
- 必须运行相关测试或验证命令，确认改动可用。
- 更新 `MODIFICATIONS.md`，记录本次修改的人类可读描述和影响的代码模块。
- 如有必要，更新本 AGENTS.md 中新增的约定。

## 3. 增量交付与产品验证规范

鉴于项目参与方包含非研发角色（产品），采用**按模块逐步交付、每轮独立可验证**的方式，避免一次性提交大量代码导致问题难以定位。

### 3.1 交付节奏
- 每轮只聚焦一个完整功能模块（如首页、结果页、图鉴页）。
- 每轮输出应是**可在微信开发者工具中打开并看到效果的代码**，而不是中间半成品。
- 真实识别/搜索等依赖第三方 API Key 的能力，先用 mock 数据跑通流程，后续再接入真实接口。

### 3.2 每轮结束必须执行
1. **代码自检**：运行相关验证命令或编译检查，确保无基础错误。
2. **`git commit`**：本轮改动单独提交，commit message 写明模块名称和核心内容。
3. **`git push`**：立即推送到 GitHub，确保远程有最新快照。
4. **更新 `MODIFICATIONS.md`**：记录本轮改动的人类可读描述、影响模块、验证方式。
5. **提供验证清单**：用非技术语言列出"你如何确认这轮没问题"。

### 3.3 验证清单格式

每轮交付时，向用户提供如下格式的验证说明：

```
本轮完成：xxx 页面/功能
验证步骤：
1. 用微信开发者工具打开 curious-field-guide 文件夹
2. 点击"编译"
3. 应看到：xxx
4. 点击 xxx，应出现 xxx
当前状态：mock 数据 / 真实接口
```

### 3.4 问题排查与回滚
- 若某轮出现问题，可通过 GitHub 提交历史回滚到上一轮的提交。
- 每轮提交保持独立，避免多轮改动混入同一个 commit。

## 4. 代码质量标准

以下标准适用于所有生成的代码文件，每个文件完成后需自检。

### 4.1 敏感信息必须隔离

- API Key、AppID、Secret、Token 等敏感字符串不得出现在业务逻辑文件中。
- 敏感配置集中管理；业务文件通过引用方式获取配置，不直接写死。
- 敏感配置文件（如 `.env`、本地配置）不得提交到版本控制。

### 4.2 不要重复自己（DRY）

- 同样的逻辑不得复制粘贴到多个文件中。
- 公共操作（请求、日期格式化、本地存储）应抽象为可复用工具函数。
- 每个工具函数职责单一，页面逻辑保持简洁。

### 4.3 数据流要一眼看懂

- 数据从哪来、经过什么处理、存到哪、怎么展示，应在同一文件或相邻文件中可追踪。
- 用户操作 → 请求数据 → 处理数据 → 存储数据 → 更新界面的链条应清晰。
- 每个页面的数据状态应有中文注释说明用途。

### 4.4 样式要统一

- 颜色、字号、间距等视觉标准在 `app.wxss` / `utils/constants.js` 中统一定义。
- 页面级样式只负责布局位置，不得重新定义视觉标准。

### 4.5 组件要复用

- 多个页面出现的相同 UI 片段应抽成组件。
- 组件独立、自包含，不依赖特定页面的上下文。

### 4.6 命名要诚实

- 文件名和变量名应一眼能看出用途。
- 禁止 `a`、`b`、`temp`、`test`、`demo`、`newfile` 等无意义命名。
- 文件名使用英文小写，单词间用连字符 `-` 分隔。

### 4.7 注释要说明"为什么"和"做什么"

- 每个函数前应有中文注释，说明用途、参数、返回值。
- 复杂逻辑加行内注释解释思路。
- 不写"显而易见"的注释，写给"三个月后回头看"的自己。

### 4.8 生成后自检

每完成一个文件或模块，需检查：

1. 这个文件里有没有不该出现的敏感字符串？
2. 这个逻辑有没有可能在别的地方已经写过了？
3. 如果我不是作者，我能一眼看懂这个数据是怎么流动的吗？
4. 这个颜色/字号是不是在别的地方定义过？
5. 这个命名，三个月后我还能看懂吗？

以上任何一条答案为"否"或"不确定"，需修正后再继续。

## 5. 修改日志要求

每次代码改动完成后，必须同步写入 `MODIFICATIONS.md`，格式如下：

```markdown
## YYYY-MM-DD HH:MM

- **描述**：用人类能听懂的话说明改了什么，例如“修复了登录时密码验证失败的问题”。
- **模块**：列出被修改的主要文件或模块，例如 `src/auth/login.py`、`tests/test_login.py`。
- **备注**：可选，补充测试命令、回滚注意点等。
```

## 6. 版本控制（Git / GitHub）

- 每次有意义的代码改动完成后，尽量做一次 `git commit`。
- Commit message 用中文或英文均可，需简洁说明改动内容。
- 推送到 GitHub 需要用户提前配置仓库和授权；Kimi 不会在未确认时执行 push、创建远程仓库等操作。
- 每次 commit 前不执行 `git add .` 全量添加，而是只添加本次改动的文件，避免把无关临时文件带入。

## 7. 项目约定

- 项目根目录：`/Users/dadtao/kimi code`
- 小程序项目目录：`/Users/dadtao/kimi code/curious-field-guide`
- 日志文件：`MODIFICATIONS.md`
- 规范文件：`AGENTS.md`
