<!-- 稀有度标签组件 -->
<!-- 说明：接收标签数组，按统一样式渲染胶囊标签 -->

<view class="rarity-tags" wx:if="{{tags.length > 0}}">
  <view
    wx:for="{{tags}}"
    wx:key="index"
    class="rarity-tag tag tag-{{item.color}}"
  >
    {{item.label}}
  </view>
</view>
